# STM32 Start

本文档基于 STM32F407 平台，详细梳理了从硬件上电复位到进入 C 语言 `main()` 函数的全过程。

## 一、 硬件复位与时序逻辑 (Hardware Reset)

当 STM32 芯片上电或复位引脚触发电平变化时，Cortex-M4 内核首先接管控制权。根据启动引脚（Boot0/1）的配置（通常指向 Flash），内核会自动从内存地址 `0x08000000` 执行以下操作：

1.  **加载初始堆栈指针 (MSP)**：
    内核从 `0x08000000` 读取第一个 32 位数据，将其加载到主堆栈指针寄存器（SP）。在 `STM32F407XX_FLASH.ld` 链接脚本中，该值通常映射为 `_estack`，即内部 SRAM 的末尾地址。
2.  **加载程序计数器 (PC)**：
    内核紧接着从 `0x08000004` 读取第二个 32 位数据，将其加载到程序计数器（PC）。这个地址指向了复位处理函数 `Reset_Handler` 的入口点。

### 为什么硬件必须先加载 SP 和 PC？

*   **PC (“第一道指令”)**：没有 PC，CPU 无法取指执行代码。硬件规定了初始地址，确保 CPU 知道从哪里开始运行。
*   **SP (“工作台”)**：Cortex-M 架构要求系统在运行第一行代码前就具备处理异常的能力。如果发生硬件故障，自动压栈操作必须有一个合法的堆栈指针。此外，有了 SP，理论上可以直接用纯 C 语言编写启动代码，因为 C 语言的函数调用和局部变量强依赖堆栈。

## 二、 CPU 向量表 (Vector Table)

硬件刚才读取的 `0x08000000` 和 `0x08000004`，正是 **CPU 向量表** 的前两项。

向量表是一张存储“函数地址”的“索引表”。当特定的事件（如复位、中断、硬件故障）发生时，硬件会自动查表跳转到对应的处理函数，这是硬件与软件之间的核心契约。

**结构示例：**
```c
uint32_t VectorTable[] = {
    0x20020000,    // [0] 初始堆栈指针 (MSP)
    0x08000101,    // [1] 复位处理函数地址 (Reset_Handler)
    0x08000451,    // [2] 不可屏蔽中断地址 (NMI_Handler)
    0x08000560,    // [3] 硬件故障地址 (HardFault_Handler)
    ...
    0x08000B00,    // [16] UART1 中断地址
};
```
它是“向量化中断”的基石，确保了极高的响应效率，无需额外的软件分支判断。

## 三、 汇编启动阶段 (`startup_stm32f407xx.s`)

程序跳转到 `Reset_Handler` 后，执行的是汇编代码，这是 C 语言运行环境搭建的关键步骤：

1.  **设置堆栈指针**：再次确保 SP 被正确初始化（`ldr sp, =_estack`）。
2.  **系统初始化预处理**：执行 `bl SystemInit`，在搬运数据前配置时钟等基础系统环境。
3.  **搬运 `.data` 段**：将编译时存储在 Flash 中的已初始化全局/静态变量（例如 `int a = 10;`）复制到 RAM 中（从 `_sidata` 到 `_sdata` 区域）。
4.  **清零 `.bss` 段**：将未初始化的全局/静态变量所在 RAM 区域全清零（从 `_sbss` 到 `_ebss` 区域）。
5.  **调用 `__libc_init_array`**：初始化 C 标准库和全局对象（详见下文）。
6.  **跳转主程序**：执行 `bl main`，正式进入用户业务代码空间。

## 四、 `__libc_init_array` 的作用

在搬运数据和清理内存完毕后，跳转 `main` 函数之前，必须执行 `__libc_init_array`。它是标准 C 库（如 Newlib）提供的重要函数。

**核心任务**：遍历并执行所有的“构造函数”或“初始化函数”。
*   **对于 C++**：它负责调用所有全局对象的构造函数，确保这些对象在被 `main` 调用前已完成初始化。如果缺少这一步，程序将发生致命错误。
*   **对于 C 语言**：它负责触发通过 `__attribute__((constructor))` 修饰的函数，允许在 `main` 运行前执行特定的初始化逻辑。

**执行时机**：它必须放在数据搬运（`.data` 初始化）之后执行。因为构造函数通常会修改全局变量，如果提前执行，其结果会被后续的数据搬运覆盖。

## 五、 系统初始化与主程序执行

### 系统底层初始化 (`system_stm32f4xx.c`)
汇编阶段调用的 `SystemInit()` 主要负责：
*   **重置时钟**：将 RCC 寄存器恢复到默认状态（HSI）。
*   **配置向量表偏移 (VTOR)**：指示向量表在 Flash 中的具体位置。
*   **FPU 初始化**：使能硬件浮点运算单元。

### 业务初始化 (`main.c`)
进入 `main()` 函数后，依次执行：
1.  **`HAL_Init()`**：初始化 HAL 库（配置 Flash 预取、系统基准时钟 SysTick 等）。
2.  **`SystemClock_Config()`**：配置 PLL，切换至高速外部时钟（HSE），达到目标系统频率。
3.  **外设初始化**：调用各类 `MX_Init` 函数配置 GPIO、UART、CAN 等外设。
4.  **RTOS 初始化与启动**：创建任务（`MX_FREERTOS_Init()`），调用 `osKernelStart()`，至此 CPU 控制权移交操作系统，系统全面运行。
