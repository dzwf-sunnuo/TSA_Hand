# 灵巧手 CAN 总线通信协议

**CAN ID**: `CAN_HAND_ID` — `0x020` (右手) 或 `0x021` (左手)，编译时二选一  
**帧格式**: 标准帧 (11-bit ID)，数据场 1~8 字节  
**字节序**: 16 位子命令为大端 (`data[1]` 高字节, `data[2]` 低字节)  
**实现位置**: `Core/Src/app_comm_tasks.c`

---

## 1. 心跳检测 — `data[0] = 0x00`

| 方向 | data[0] | 说明 |
|---|---|---|
| 上位机 → 手 | `0x00` | 心跳请求 |
| 手 → 上位机 | `0x01` | 心跳应答 (共用同一 CAN ID) |

帧长 1 字节。在 `Task_CAN_Process_Entry` 中最先判断，不进入动作分发。

---

## 2. 手腕摆动控制 — `data[0] = 0x10`

子命令 16 位大端 (`data[1..2]`)，除 `0x0005` 外均无附加数据。

| 子命令 | 功能 | 等价 CAN 帧 |
|---|---|---|
| `0x0000` | 停止连续摆动 | `10 00 00` |
| `0x0001` | 启动连续摆动 (S 曲线往复) | `10 00 01` |
| `0x0002` | 单次：最左侧 | `10 00 02` |
| `0x0003` | 单次：最右侧 | `10 00 03` |
| `0x0004` | 单次：回中 | `10 00 04` |
| `0x0005` | 设置摆动周期 | `10 00 05 cyc_H cyc_L` |

**周期设置** (`0x0005`): `data[3..4]` = `cycle_ms` (uint16 大端, 单位 ms)。

调用链: `CAN_Msg_MapToAction` → `Action_Dispatch` → `WristMotion_Start/Stop`

---

## 3. 手腕旋转控制 — `data[0] = 0x11`

子命令格式同 `0x10`。

| 子命令 | 功能 |
|---|---|
| `0x0000` | 停止连续旋转 |
| `0x0001` | 启动连续旋转 (四象限梯形速度) |
| `0x0002` | 预设位置 1 |
| `0x0003` | 预设位置 2 |
| `0x0004` | 预设位置 3 |
| `0x0005` | 设置旋转周期 (`data[3..4]` = `cycle_ms` 大端) |

---

## 4. 手指连续动作 — `data[0] = 0x12`

子命令 16 位大端 (`data[1..2]`)，`data[3]` = 循环次数。

| 子命令 | 功能 | 帧数 | 默认速度 |
|---|---|---|---|
| `0x0000` | 停止 | — | — |
| `0x0001` | 连续弯折 | 9 | 300ms/帧 |
| `0x0002` | 数数字 (0-9) | 13 | 1300ms/帧 |
| `0x0003` | 连续握拳 | 3 | 1500ms/帧 |

调用链: `CAN_Msg_MapToAction` → `Action_Dispatch` → `finger_motion()`

---

## 5. 手指单个动作 — `data[0] = 0x13` / `0x14`

子命令 16 位大端 = 手势编号。两指令功能相同。

| 编号 | 手势 |
|---|---|
| `0x0000` | 展开 |
| `0x0001` | 握手 |
| `0x0002`~`0x0009` | 数字 2~9 |
| `0x000A` | 拳 |
| `0x000B` | OK |

调用链: `CAN_Msg_MapToAction` → `Action_Dispatch` → `finger_single_action()`

---

## 6. 直接硬件指令

以下指令**不走** `CAN_Msg_MapToAction` / `Action_Dispatch`，在 `Task_CAN_Process_Entry` 中直接处理。

### 6.1 全电机正转 — `data[0] = 0x15`

帧长 8 字节，格式同 `0x16`。mode=1 (正转)。

### 6.2 全电机反转 — `data[0] = 0x16`

```
0x16  j1    j2    j3      j4    j5    j6    j7
      └── ban1 (3电机) ──┘└── ban2 (4电机) ──┘
```

| data[n] | 关节 | 从板 | 寄存器 |
|---|---|---|---|
| `data[1]` | 关节 01 | ban1 (0x01) | Reg[4] |
| `data[2]` | 关节 02 | ban1 | Reg[5] |
| `data[3]` | 关节 03 | ban1 | Reg[6] |
| `data[4]` | 关节 04 | ban2 (0x02) | Reg[9] |
| `data[5]` | 关节 05 | ban2 | Reg[10] |
| `data[6]` | 关节 06 | ban2 | Reg[11] |
| `data[7]` | 关节 07 | ban2 | Reg[12] |

- 固定速度 `0x64`
- `0x15` = mode=1 正转 (Reg[8]=0x0101, Reg[13]=0x0101)
- `0x16` = mode=3 反转 (Reg[8]=0x0301, Reg[13]=0x0301)
- 寄存器值 = `(圈数 << 8) | 0x64`
- 同时下发两块从板，间隔 15ms

### 6.3 编码器清零 — `data[0] = 0x17`

帧长 1 字节，无附加数据。所有电机圈数归零 (mode=4)。

```
0x17
```

### 6.4 推杆电机定位 — `data[0] = 0x18`

```
0x18  p1_H p1_L  p2_H p2_L
      └─pos1──┘ └─pos2──┘
```

| data[n] | 电机 | 接口 |
|---|---|---|
| `data[1..2]` | 手腕推杆 1 | USART3, `vel_move()` |
| `data[3..4]` | 手腕推杆 2 | USART6, `vel_move1()` |

- 速度固定 `0x0DAC` (3500, ~28mm/s)
- 位置范围: `0x0000`~`0x07D0` (0~2000 steps, 0~16mm)

---

## 7. 指令总览

| data[0] | 功能 | 处理方式 |
|---|---|---|
| `0x00` | 心跳请求/应答 | 直接 I/O |
| `0x10` | 手腕摆动控制 | Map → Dispatch |
| `0x11` | 手腕旋转控制 | Map → Dispatch |
| `0x12` | 手指连续动作 | Map → Dispatch |
| `0x13` | 手指单个动作 | Map → Dispatch |
| `0x14` | 手指单个动作 | Map → Dispatch |
| `0x15` | 全电机正转 (mode=1) | 直接硬件 |
| `0x16` | 全电机反转 (mode=3) | 直接硬件 |
| `0x17` | 编码器清零 (mode=4) | 直接硬件 |
| `0x18` | 推杆电机定位 | 直接硬件 |

---

## 8. 帧示例

```
# 心跳
上位机: 020#00
手回复: 020#01

# 启动手腕摆动 (周期 3200ms)
上位机: 020#100001

# 设置摆动周期 4000ms (0x0FA0)
上位机: 020#1000050FA0

# 手指连续弯折 3 遍
上位机: 020#12000103

# 单个手势 OK (0x000B)
上位机: 020#13000B

# 全电机正转: 关节1=1圈, ... 关节7=7圈
上位机: 020#1501020304050607

# 全电机反转: 关节1=1圈, ... 关节7=7圈
上位机: 020#1601020304050607

# 编码器清零
上位机: 020#17

# 推杆定位: motor1=0x002C(44), motor2=0x07D0(2000)
上位机: 020#18002C07D0
```
